---
title: "Available Matrix"
output: 
  pdf_document: 
    toc: yes
editor_options: 
  chunk_output_type: console
---

```{r}
#| echo: false
#| eval: true
set.seed(999)
knitr::opts_chunk$set(echo=FALSE, 
                      eval=TRUE, 
                      warning = F, 
                      message = F, 
                      fig.align = "center")

rm(list = ls())

source("Shapes_list-10-11-Ottavia.R")
source("Class and Methods v02.R")
source("Rules_27102022.R")
source("DrawRegPolygon.R")
source("CodiceDistrattoriVero.R")

draws<- function(obj, main = NULL, canvas = TRUE, bg = "white",mar=c(1,1,1,1),xlim=16,by=1.5) {
  library(DescTools)
  if (canvas == TRUE)
  {
    Canvas(xlim=xlim,mar=mar, main = main, bg = bg)
  }
  for(j in 1:length(obj$shape))
  {
    if(obj$visible[[j]]==1)
    {
      if(obj$num[[j]][1]==1){
        if(grepl("line",obj$shade[[j]][1]))
        {
          elements<-decof(obj)
          plotting_lines<-which(obj$visible==1 & grepl("line",unlist(obj$shade)))
          for(ll in 1:length(plotting_lines)){
            line(elements[[plotting_lines[[ll]]]],obj$shade[[j]][1],lwd=3,by=by) #Pejo tacon che sbrego
            
          }
          obj$shade[[j]][1] <- NA
        }
        DrawRegPolygon(x = obj$pos.x[[j]], y = obj$pos.y[[j]], rot = obj$rotation[[j]],
                       radius.x = obj$size.x[[j]], radius.y = obj$size.y[[j]], nv = obj$nv[[j]],
                       lty=obj$lty[[j]],lwd=obj$lwd[[j]],col = obj$shade[[j]])
        
        
      }else{
        
        DrawCircle(x = obj$pos.x[[j]], y = obj$pos.y[[j]],
                   r.out = obj$size.x[[j]],r.in= obj$size.y[[j]], theta.1=obj$theta.1[[j]],
                   theta.2=obj$theta.2[[j]], nv = obj$nv[[j]],
                   lty=obj$lty[[j]],lwd=obj$lwd[[j]],col = obj$shade[[j]])
        
      }
    }
  }
}

lilth<-lily()
s.lilth<-s.lily()
for(i in 1:length(lilth$shape)) {
  lilth$size.x[[i]] <-lilth$size.x[[i]]/2
  lilth$size.y[[i]] <-lilth$size.y[[i]]/2
  lilth$pos.y[[i]] <-lilth$pos.y[[i]]/2
  lilth$pos.x[[i]] <-lilth$pos.x[[i]]/2
  
}

s.lilth$size.x[[1]] <-s.lilth$size.x[[1]]/2
s.lilth$size.y[[1]] <-s.lilth$size.y[[1]]/2
s.lilth$pos.y[[1]] <-s.lilth$pos.y[[1]]/2
s.lilth$pos.x[[1]] <-s.lilth$pos.x[[1]]/2

papillon = bow.tie()
u.papillon = u.bow.tie()


for(i in 1:length(papillon$shape)) {
  papillon$size.x[[i]] <-papillon$size.x[[i]]/2
  papillon$size.y[[i]] <-papillon$size.y[[i]]/2
  papillon$pos.y[[i]] <-papillon$pos.y[[i]]/2
  papillon$pos.x[[i]] <-papillon$pos.x[[i]]/2
  
}

u.papillon$size.x[[1]] <-u.papillon$size.x[[1]]/2
u.papillon$size.y[[1]] <-u.papillon$size.y[[1]]/2
u.papillon$pos.y[[1]] <-u.papillon$pos.y[[1]]/2
u.papillon$pos.x[[1]] <-u.papillon$pos.x[[1]]/2


thepie = pie.4()
u.thepie = u.pie.4()


for(i in 1:length(thepie$shape)) {
  thepie$size.x[[i]] <-thepie$size.x[[i]]/2
  thepie$size.y[[i]] <-thepie$size.y[[i]]/2
  thepie$pos.y[[i]] <-thepie$pos.y[[i]]/2
  thepie$pos.x[[i]] <-thepie$pos.x[[i]]/2
  
}

u.thepie$size.x[[1]] <-u.thepie$size.x[[1]]/2
u.thepie$size.y[[1]] <-u.thepie$size.y[[1]]/2
u.thepie$pos.y[[1]] <-u.thepie$pos.y[[1]]/2
u.thepie$pos.x[[1]] <-u.thepie$pos.x[[1]]/2

biscuit = star()
u.biscuit = u.star()


for(i in 1:length(biscuit$shape)) {
  biscuit$size.x[[i]] <-biscuit$size.x[[i]]/2
  biscuit$size.y[[i]] <-biscuit$size.y[[i]]/2
  biscuit$pos.y[[i]] <-biscuit$pos.y[[i]]/2
  biscuit$pos.x[[i]] <-biscuit$pos.x[[i]]/2
  
}

u.biscuit$size.x[[1]] <-u.biscuit$size.x[[1]]/2
u.biscuit$size.y[[1]] <-u.biscuit$size.y[[1]]/2
u.biscuit$pos.y[[1]] <-u.biscuit$pos.y[[1]]/2
u.biscuit$pos.x[[1]] <-u.biscuit$pos.x[[1]]/2


square4bis <- function() {
  value <-cof(hline(pos.y=-11),vline(pos.x=11),
              hline(pos.y=11),vline(pos.x=-11))
  value$tag <- list("compose4")
  attr(value, "class") <- "field"
  value
}


smallbow.tie.inv <- function(pos.x = 0,pos.y=0,shd=NA) {
  value <-cof(triangle(pos.x = pos.x+5, pos.y = pos.y, rot=pi/3, 
                       s.x = 5, s.y=5,shd = shd), 
              triangle(pos.x = pos.x-5, pos.y = pos.y, rot=-pi, 
                       s.x = 5, s.y=5,shd = shd))
  value$tag <- list("compose2","fill", "rotate")
  attr(value, "class") <- "field"
  value
}

bow.tie.inv <- function(pos.x = 0) {
  value <-cof(triangle(pos.x = pos.x+10, pos.y = pos.x, rot=pi/3, 
                       s.x = 10, s.y=10), 
              triangle(pos.x = pos.x-10, pos.y = pos.x, rot=-pi, 
                       s.x = 10, s.y=10))
  value$tag <- list("compose2","fill", "rotate")
  attr(value, "class") <- "field"
  value
}


```

\newpage

# Monotematiche

## Righe semplici Verticali

```{r echo=FALSE}


# vertical canvas
draw(vline(pos.x=-30, s.x = 30, lwd = 3))
for(i in seq(-30, 30, by = 1)) {
  draw(vline(pos.x = i, s.x=40, lwd = 3), 
       canvas = F)
}

draw(rectangle(s.x=7,s.y=5,shd="white",pos.x=+10,pos.y=-7),
     canvas = FALSE)

```


```{r }
par(mfrow =c(2, 3), mar = c(0.5, 6, 0.5, 2) + .1, 
          mai=c(.1,.1,.1,.1),oma=c(4,4,0.2,0.2) )
container = rectangle(s.x=7,s.y=5,shd="white")
draw(container,xlim = 8)
for(i in seq(-7, 7, by = 1)) {
 draw(vline(pos.x = i, s.x=5, lwd = 3), canvas = F)
}

draw(container,xlim = 8)
for(i in seq(-5, 5, by = 1)) {
 draw(hline(pos.y = i, s.x=7, lwd = 3), canvas = F)
}


draw(container,xlim = 8)

draw(container,xlim = 8)
for (i in seq(-6, 7, by = 3)) {
  for (j in seq(-3, 3, by = 3)) {
    draw(dot(pos.x = i, pos.y = j, size.x = 0.5, size.y = 0.5), canvas = F)
  }
}


draw(container,xlim = 8)
for(i in seq(-7, 7, by = 1)) {
 draw(vline(pos.x = i, s.x=5, lwd = 3), canvas = F)
}

for(i in seq(-5, 5, by = 1)) {
 draw(hline(pos.y = i, s.x=7, lwd = 3), canvas = F)
}


```



## Vertical color

```{r}
col = "#ffdab9"
# vertical canvas
draw(rectangle(s.x=-50,s.y=50,shd=col,pos.x=+10,pos.y=-7))

for(i in seq(-30, 30, by = 1)) {
  draw(vline(pos.x = i, s.x=40, lwd = 3), 
       canvas = F)
}

draw(rectangle(s.x=7,s.y=5,shd="white",pos.x=+10,pos.y=-7),
     canvas = FALSE)
```


```{r}
par(mfrow =c(2, 3), mar = c(0.5, 6, 0.5, 2) + .1, 
          mai=c(.1,.1,.1,.1),oma=c(4,4,0.2,0.2) )
container = rectangle(s.x=7,s.y=5,shd=col)
draw(container,xlim = 8)
for(i in seq(-7, 7, by = 1)) {
 draw(vline(pos.x = i, s.x=5, lwd = 3), canvas = F)
}

draw(container,xlim = 8)
for(i in seq(-5, 5, by = 1)) {
 draw(hline(pos.y = i, s.x=7, lwd = 3), canvas = F)
}


draw(container,xlim = 8)

draw(container,xlim = 8)
for (i in seq(-6, 7, by = 3)) {
  for (j in seq(-3, 3, by = 3)) {
    draw(square(pos.x = i, pos.y = j, s.x = 0.5, s.y = 0.5, shd = "black"), canvas = F)
  }
}


draw(container,xlim = 8)
for(i in seq(-7, 7, by = 1)) {
 draw(vline(pos.x = i, s.x=5, lwd = 3), canvas = F)
}

for(i in seq(-5, 5, by = 1)) {
 draw(hline(pos.y = i, s.x=7, lwd = 3), canvas = F)
}


```


## Horizontal

```{r echo=FALSE}
draw(hline(pos.y=-17, s.x = 30))
for(i in seq(-17, 17, by = 1)) {
  draw(hline(pos.y = i, s.x=30, lwd = 3), 
       canvas = F)
}

draw(rectangle(s.x=7,s.y=5,shd="white",pos.x=+10,pos.y=-7),
     canvas = FALSE)


```

```{r}
par(mfrow =c(2, 3), mar = c(0.5, 6, 0.5, 2) + .1, 
          mai=c(.1,.1,.1,.1),oma=c(4,4,0.2,0.2) )
container = rectangle(s.x=7,s.y=5,shd="white")

draw(container,xlim = 8)
for(i in seq(-5, 5, by = 1)) {
 draw(hline(pos.y = i, s.x=7, lwd = 3), canvas = F)
}


draw(container,xlim = 8)
for(i in seq(-7, 7, by = 1)) {
 draw(vline(pos.x = i, s.x=5, lwd = 3), canvas = F)
}



draw(container,xlim = 8)

draw(container,xlim = 8)
for (i in seq(-6, 7, by = 3)) {
  for (j in seq(-3, 3, by = 3)) {
    draw(dot(pos.x = i, pos.y = j, size.x = 0.5, size.y = 0.5), canvas = F)
  }
}


draw(container,xlim = 8)
for(i in seq(-7, 7, by = 1)) {
 draw(vline(pos.x = i, s.x=5, lwd = 3), canvas = F)
}

for(i in seq(-5, 5, by = 1)) {
 draw(hline(pos.y = i, s.x=7, lwd = 3), canvas = F)
}


```




## horizontal color

```{r}
draw(rectangle(s.x=-50,s.y=50,shd=col,pos.x=+10,pos.y=-7))
for(i in seq(-17, 17, by = 1)) {
  draw(hline(pos.y = i, s.x=30, lwd = 3), 
       canvas = F)
}

draw(rectangle(s.x=7,s.y=5,shd="white",pos.x=+10,pos.y=-7),
     canvas = FALSE)
```

```{r }
par(mfrow =c(2, 3), mar = c(0.5, 6, 0.5, 2) + .1, 
          mai=c(.1,.1,.1,.1),oma=c(4,4,0.2,0.2) )
container = rectangle(s.x=7,s.y=5,shd=col)
draw(container,xlim = 8)
for(i in seq(-5, 5, by = 1)) {
 draw(hline(pos.y = i, s.x=7, lwd = 3), canvas = F)
}



draw(container,xlim = 8)
for(i in seq(-7, 7, by = 1)) {
 draw(vline(pos.x = i, s.x=5, lwd = 3), canvas = F)
}



draw(container,xlim = 8)

draw(container,xlim = 8)
for (i in seq(-6, 7, by = 3)) {
  for (j in seq(-3, 3, by = 3)) {
    draw(square(pos.x = i, pos.y = j, s.x = 0.5, s.y = 0.5, shd = "black"), canvas = F)
  }
}


draw(container,xlim = 8)
for(i in seq(-7, 7, by = 1)) {
 draw(vline(pos.x = i, s.x=5, lwd = 3), canvas = F)
}

for(i in seq(-5, 5, by = 1)) {
 draw(hline(pos.y = i, s.x=7, lwd = 3), canvas = F)
}


```




## Insieme

```{r}

draw(vline(pos.x=-30, s.x = 30, lwd = 3))
for(i in seq(-30, 30, by = 1)) {
  draw(vline(pos.x = i, s.x=40, lwd = 3), 
       canvas = F)
}
for(i in seq(-17, 17, by = 1)) {
  draw(hline(pos.y = i, s.x=30, lwd = 3), 
       canvas = F)
}

draw(rectangle(s.x=7,s.y=5,shd="white",pos.x=+10,pos.y=-7),
     canvas = FALSE)
```

```{r}
par(mfrow =c(2, 3), mar = c(0.5, 6, 0.5, 2) + .1, 
          mai=c(.1,.1,.1,.1),oma=c(4,4,0.2,0.2) )
container = rectangle(s.x=7,s.y=5,shd="white")

draw(container,xlim = 8)
for(i in seq(-7, 7, by = 1)) {
 draw(vline(pos.x = i, s.x=5, lwd = 3), canvas = F)
}

for(i in seq(-5, 5, by = 1)) {
 draw(hline(pos.y = i, s.x=7, lwd = 3), canvas = F)
}

draw(container,xlim = 8)
for(i in seq(-5, 5, by = 1)) {
 draw(hline(pos.y = i, s.x=7, lwd = 3), canvas = F)
}


draw(container,xlim = 8)
for(i in seq(-7, 7, by = 1)) {
 draw(vline(pos.x = i, s.x=5, lwd = 3), canvas = F)
}



draw(container,xlim = 8)

draw(container,xlim = 8)
for (i in seq(-6, 7, by = 3)) {
  for (j in seq(-3, 3, by = 3)) {
    draw(dot(pos.x = i, pos.y = j, size.x = 0.5, size.y = 0.5), canvas = F)
  }
}

```



## Insieme color

```{r}

draw(rectangle(s.x=-50,s.y=50,shd=col,pos.x=+10,pos.y=-7))
for(i in seq(-30, 30, by = 1)) {
  draw(vline(pos.x = i, s.x=40, lwd = 3), 
       canvas = F)
}
for(i in seq(-17, 17, by = 1)) {
  draw(hline(pos.y = i, s.x=30, lwd = 3), 
       canvas = F)
}


draw(rectangle(s.x=7,s.y=5,shd="white",pos.x=+10,pos.y=-7),
     canvas = FALSE)
```


```{r}
par(mfrow =c(2, 3), mar = c(0.5, 6, 0.5, 2) + .1, 
          mai=c(.1,.1,.1,.1),oma=c(4,4,0.2,0.2) )
container = rectangle(s.x=7,s.y=5,shd= col)

draw(container,xlim = 8)
for(i in seq(-7, 7, by = 1)) {
 draw(vline(pos.x = i, s.x=5, lwd = 3), canvas = F)
}

for(i in seq(-5, 5, by = 1)) {
 draw(hline(pos.y = i, s.x=7, lwd = 3), canvas = F)
}

draw(container,xlim = 8)
for(i in seq(-5, 5, by = 1)) {
 draw(hline(pos.y = i, s.x=7, lwd = 3), canvas = F)
}


draw(container,xlim = 8)
for(i in seq(-7, 7, by = 1)) {
 draw(vline(pos.x = i, s.x=5, lwd = 3), canvas = F)
}



draw(container,xlim = 8)

draw(container,xlim = 8)
for (i in seq(-6, 7, by = 3)) {
  for (j in seq(-3, 3, by = 3)) {
    draw(dot(pos.x = i, pos.y = j, size.x = 0.5, size.y = 0.5), canvas = F)
  }
}

```


## Diagonale principale

```{r}

draw(diagline(pos.x=-50, s.x = 50, lwd = 3))
for(i in seq(-50, 50, by = 1.5)) {
  draw(diagline(pos.x = i, s.x=40, lwd = 3), 
       canvas = F)
}


draw(rectangle(s.x=7,s.y=5,shd="white",pos.x=+10,pos.y=-7),
     canvas = FALSE)

```



```{r}
par(mfrow =c(2, 3), mar = c(0.5, 6, 0.5, 2) + .1, 
          mai=c(.1,.1,.1,.1),oma=c(4,4,0.2,0.2) )

container = rectangle(s.x=7,s.y=5,shd="white")

draw(container,xlim = 8)
draws(rectangle(s.x=7,s.y=5,shd="line.12.inv"),
     canvas = FALSE)

draw(container,xlim = 8)
draws(rectangle(s.x=7,s.y=5,shd="line.12"),
     canvas = FALSE)

draw(container,xlim = 8)

draw(container,xlim = 8)
for (i in seq(-6, 7, by = 3)) {
  for (j in seq(-3, 3, by = 3)) {
    draw(dot(pos.x = i, pos.y = j, size.x = 0.5, size.y = 0.5), canvas = F)
  }
}


draw(container,xlim = 8)
for(i in seq(-7, 7, by = 1)) {
 draw(vline(pos.x = i, s.x=5, lwd = 3), canvas = F)
}

for(i in seq(-5, 5, by = 1)) {
 draw(hline(pos.y = i, s.x=7, lwd = 3), canvas = F)
}






```


## Diagonale secondaria

```{r}

draw(diagline.inv(pos.x=-50, s.x = 50, lwd = 3))
for(i in seq(-50, 50, by = 1.5)) {
  draw(diagline.inv(pos.x = i, s.x=40, lwd = 3), 
       canvas = F)
}


draw(rectangle(s.x=7,s.y=5,shd="white",pos.x=+10,pos.y=-7),
     canvas = FALSE)
```
```{r}
par(mfrow =c(2, 3), mar = c(0.5, 6, 0.5, 2) + .1, 
          mai=c(.1,.1,.1,.1),oma=c(4,4,0.2,0.2) )

container = rectangle(s.x=7,s.y=5,shd="white")

draw(container,xlim = 8)
draws(rectangle(s.x=7,s.y=5,shd="line.12"),
     canvas = FALSE)

draw(container,xlim = 8)
draws(rectangle(s.x=7,s.y=5,shd="line.12.inv"),
     canvas = FALSE)

draw(container,xlim = 8)

draw(container,xlim = 8)
for (i in seq(-6, 7, by = 3)) {
  for (j in seq(-3, 3, by = 3)) {
    draw(dot(pos.x = i, pos.y = j, size.x = 0.5, size.y = 0.5), canvas = F)
  }
}


draw(container,xlim = 8)
for(i in seq(-7, 7, by = 1)) {
 draw(vline(pos.x = i, s.x=5, lwd = 3), canvas = F)
}

for(i in seq(-5, 5, by = 1)) {
 draw(hline(pos.y = i, s.x=7, lwd = 3), canvas = F)
}


```

## Insieme (mal di mare)

```{r}
draw(diagline(pos.x=-50, s.x = 50, lwd = 3))
for(i in seq(-50, 50, by = 1.5)) {
  draw(diagline(pos.x = i, s.x=40, lwd = 3), 
       canvas = F)
}


draw(diagline.inv(pos.x=-50, s.x = 50, lwd = 3), canvas = F)
for(i in seq(-50, 50, by = 1.5)) {
  draw(diagline.inv(pos.x = i, s.x=40, lwd = 3), 
       canvas = F)
}


draw(rectangle(s.x=7,s.y=5,shd="white",pos.x=+10,pos.y=-7),
     canvas = FALSE)
```

```{r}
par(mfrow =c(2, 3), mar = c(0.5, 6, 0.5, 2) + .1, 
          mai=c(.1,.1,.1,.1),oma=c(4,4,0.2,0.2) )

container = rectangle(s.x=7,s.y=5,shd="white")

draw(container,xlim = 8)
draws(rectangle(s.x=7,s.y=5,shd="line.12.inv"),
     canvas = FALSE)

draws(rectangle(s.x=7,s.y=5,shd="line.12"),
     canvas = FALSE)

draw(container,xlim = 8)
draws(rectangle(s.x=7,s.y=5,shd="line.12"),
     canvas = FALSE)
draws(rectangle(s.x=7,s.y=5,shd="line.12.inv"),by=3.5,
     canvas = FALSE)

draw(container,xlim = 8)

draw(container,xlim = 8)
for (i in seq(-6, 7, by = 3)) {
  for (j in seq(-3, 3, by = 3)) {
    draw(dot(pos.x = i, pos.y = j, size.x = 0.5, size.y = 0.5), canvas = F)
  }
}


draw(container,xlim = 8)
for(i in seq(-7, 7, by = 1)) {
 draw(vline(pos.x = i, s.x=5, lwd = 3), canvas = F)
}

for(i in seq(-5, 5, by = 1)) {
 draw(hline(pos.y = i, s.x=7, lwd = 3), canvas = F)
}


```

## Si può variare la distanza

```{r}

draw(diagline(pos.x=-50, s.x = 50, lwd = 3))
for(i in seq(-50, 50, by = 3.5)) {
  draw(diagline(pos.x = i, s.x=40, lwd = 3), 
       canvas = F)
}


draw(diagline.inv(pos.x=-50, s.x = 50, lwd = 3), canvas = F)
for(i in seq(-50, 50, by = 3.5)) {
  draw(diagline.inv(pos.x = i, s.x=40, lwd = 3), 
       canvas = F)
}


draw(rectangle(s.x=7,s.y=5,shd="white",pos.x=+10,pos.y=-7),
     canvas = FALSE)
```

```{r}
par(mfrow =c(2, 3), mar = c(0.5, 6, 0.5, 2) + .1, 
          mai=c(.1,.1,.1,.1),oma=c(4,4,0.2,0.2) )

container = rectangle(s.x=7,s.y=5,shd="white")

draw(container,xlim = 8)
draws(rectangle(s.x=7,s.y=5,shd="line.12.inv"),by=3.5,
     canvas = FALSE)

draws(rectangle(s.x=7,s.y=5,shd="line.12"),by=3.5,
     canvas = FALSE)

draw(container,xlim = 8)
draws(rectangle(s.x=7,s.y=5,shd="line.12"),
     canvas = FALSE)
draws(rectangle(s.x=7,s.y=5,shd="line.12.inv"),
     canvas = FALSE)

draw(container,xlim = 8)

draw(container,xlim = 8)
for (i in seq(-6, 7, by = 3)) {
  for (j in seq(-3, 3, by = 3)) {
    draw(dot(pos.x = i, pos.y = j, size.x = 0.5, size.y = 0.5), canvas = F)
  }
}


draw(container,xlim = 8)
for(i in seq(-7, 7, by = 1)) {
 draw(vline(pos.x = i, s.x=5, lwd = 3), canvas = F)
}

for(i in seq(-5, 5, by = 1)) {
 draw(hline(pos.y = i, s.x=7, lwd = 3), canvas = F)
}


```

## Insieme diagonali color

```{r}
draw(rectangle(s.x=-50,s.y=50,shd=col,pos.x=+10,pos.y=-7))
for(i in seq(-50, 50, by = 3.5)) {
  draw(diagline(pos.x = i, s.x=40, lwd = 3), 
       canvas = F)
}


draw(diagline.inv(pos.x=-50, s.x = 50, lwd = 3), canvas = F)
for(i in seq(-50, 50, by = 3.5)) {
  draw(diagline.inv(pos.x = i, s.x=40, lwd = 3), 
       canvas = F)
}


draw(rectangle(s.x=7,s.y=5,shd="white",pos.x=+10,pos.y=-7),
     canvas = FALSE)
```

```{r}
par(mfrow =c(2, 3), mar = c(0.5, 6, 0.5, 2) + .1, 
          mai=c(.1,.1,.1,.1),oma=c(4,4,0.2,0.2) )

container = rectangle(s.x=7,s.y=5,shd=col)

draw(container,xlim = 8)
draws(rectangle(s.x=7,s.y=5,shd="line.12.inv"),by=3.5,
     canvas = FALSE)

draws(rectangle(s.x=7,s.y=5,shd="line.12"),by=3.5,
     canvas = FALSE)

draw(container,xlim = 8)
draws(rectangle(s.x=7,s.y=5,shd="line.12"),
     canvas = FALSE)
draws(rectangle(s.x=7,s.y=5,shd="line.12.inv"),
     canvas = FALSE)

draw(container,xlim = 8)

draw(container,xlim = 8)
for (i in seq(-6, 7, by = 3)) {
  for (j in seq(-3, 3, by = 3)) {
    draw(dot(pos.x = i, pos.y = j, size.x = 0.5, size.y = 0.5), canvas = F)
  }
}


draw(container,xlim = 8)
for(i in seq(-7, 7, by = 1)) {
 draw(vline(pos.x = i, s.x=5, lwd = 3), canvas = F)
}

for(i in seq(-5, 5, by = 1)) {
 draw(hline(pos.y = i, s.x=7, lwd = 3), canvas = F)
}


```


## Più complesse

```{r}


draw(vline(pos.x = -20, s.x = 50, lwd = 3))
draw(vline(pos.x = 20, s.x = 50, lwd = 3, lty = 3), canvas = F)
draw(vline(pos.x = 17, s.x = 50, lwd = 3), canvas = F)

draw(hline(pos.y = -9, s.x = 50, lwd = 3, lty = 1), canvas = F)
draw(hline(pos.y = -12, s.x = 50, lwd = 3, lty = 3), canvas = F)


draw(rectangle(s.x=7,s.y=5,shd="white",pos.x=+15,pos.y=-10),
     canvas = FALSE)
```


```{r}
par(mfrow =c(2, 3), mar = c(0.5, 6, 0.5, 2) + .1, 
          mai=c(.1,.1,.1,.1),oma=c(4,4,0.2,0.2) )

container = rectangle(s.x=7,s.y=5,shd="white")

draw(container,xlim = 8)

draw(rectangle(s.x=7,s.y=5,shd=NA,pos.x=0,pos.y=0),
     canvas = FALSE)

draw(vline(pos.x = 5, pos.y = -0, 
           s.x  = 5, lwd = 3, lty = 3), canvas = F)
draw(vline(pos.x = 2, pos.y = 0, 
           s.x = 5, lwd = 3), canvas = F)

draw(hline(pos.y = 0, 
           pos.x = 0,  s.x = 7, lwd = 3, lty = 1), canvas = F)
draw(hline(pos.y = -3, pos.x = 0, s.x = 7, lwd = 3, lty = 3), canvas = F)

##primo distrattore
draw(container,xlim = 8)

draw(rectangle(s.x=7,s.y=5,shd=NA,pos.x=0,pos.y=0),
     canvas = FALSE)

draw(vline(pos.x = 5, pos.y = -0, 
           s.x  = 5, lwd = 3, lty = 1), canvas = F)
draw(vline(pos.x = 2, pos.y = 0, 
           s.x = 5, lwd = 3), canvas = F)

draw(hline(pos.y = 0, 
           pos.x = 0,  s.x = 7, lwd = 3, lty = 1), canvas = F)
draw(hline(pos.y = -3, pos.x = 0, s.x = 7, lwd = 3, lty = 1), canvas = F)

##Secondo distrattore

draw(container,xlim = 8)

draw(rectangle(s.x=7,s.y=5,shd=NA,pos.x=0,pos.y=0),
     canvas = FALSE)

draw(vline(pos.x = -5, pos.y = -0, 
           s.x  = 5, lwd = 3, lty = 3), canvas = F)
draw(vline(pos.x = -2, pos.y = 0, 
           s.x = 5, lwd = 3), canvas = F)

draw(hline(pos.y = 0, 
           pos.x = 0,  s.x = 7, lwd = 3, lty = 1), canvas = F)
draw(hline(pos.y = 3, pos.x = 0, s.x = 7, lwd = 3, lty = 3), canvas = F)

##terzo distrattore
draw(container,xlim = 8)
draws(rectangle(s.x=7,s.y=5,shd="line.12.inv"),by=3.5,
     canvas = FALSE)

##quarto
draw(container,xlim = 8)

```



<!-- ## In diagonale -->

<!-- ```{r} -->

<!-- draw(diagline(pos.x = -35, s.x = 50)) -->

<!-- for (i in seq(-32, - 10, by = 3)) { -->

<!--   draw(diagline(pos.x = i, s.x = 50), canvas = F) -->

<!-- } -->

<!-- draw(diagline(pos.x = 35, s.x = 50), canvas = F) -->

<!-- for (i in seq(10, 32, by = 3)) { -->

<!--   draw(diagline(pos.x = i, s.x = 50), canvas = F) -->

<!-- } -->

<!-- draw(diagline.inv(pos.x = 35, s.x = 50), canvas = F) -->

<!-- for (i in seq(10, 32, by = 3)) { -->

<!--   draw(diagline.inv(pos.x = i, s.x = 50), canvas = F) -->

<!-- } -->

<!-- draw(diagline.inv(pos.x = -35, s.x = 50), canvas = F) -->

<!-- for (i in seq(-32, -10, by = 3)) { -->

<!--   draw(diagline.inv(pos.x = i, s.x = 50), canvas = F) -->

<!-- } -->

<!-- draw(rectangle(s.x=7,s.y=5,shd="white",pos.x=+10,pos.y=-7), -->

<!--      canvas = FALSE) -->

<!-- ``` -->

## Con altre forme

```{r}
draw(dot(pos.x = -25, pos.y = 15, size.x = 2, size.y = 2))

for (i in seq(-25, 25, by = 3)) {
  for (j in seq(15, -15, by = -3)) {
    draw(dot(pos.x = i, pos.y = j, size.x = 2, size.y = 2), canvas = F)
  }
}

draw(rectangle(s.x=7,s.y=5,shd="white",pos.x=+15,pos.y=-10),
     canvas = FALSE)
```


```{r}
par(mfrow =c(2, 3), mar = c(0.5, 6, 0.5, 2) + .1, 
          mai=c(.1,.1,.1,.1),oma=c(4,4,0.2,0.2) )

container = rectangle(s.x=7,s.y=5,shd="white")

draw(container,xlim = 8)
clip(7,-7,5,-5)
draw(rectangle(s.x=7,s.y=5,shd="white",pos.x=0,pos.y=0),
     canvas = FALSE)
for (i in seq(-7, 10, by = 3)) {
  for (j in seq(-5, 5, by = 3)) {
    draw(dot(pos.x = i, pos.y = j, size.x = 2, size.y = 2), canvas = F)
  }
}

draw(container,xlim = 8)
clip(7,-7,5,-5)
draw(rectangle(s.x=7,s.y=5,shd="white",pos.x=0,pos.y=0),
     canvas = FALSE)
for (i in seq(-7, 10, by = 3)) {
  for (j in seq(-5, 5, by = 3)) {
    draw(circle(pos.x = i, pos.y = j, s.x = 2, s.y = 2,shd="white"), canvas = F)
  }
}

draw(container,xlim = 8)

draw(container,xlim = 8)
draws(rectangle(s.x=7,s.y=5,shd="line.12.inv"),by=3.5,
     canvas = FALSE)

draw(container,xlim = 8)
draws(rectangle(s.x=7,s.y=5,shd="line.12.inv"),by=3.5,
     canvas = FALSE)
draws(rectangle(s.x=7,s.y=5,shd="line.12"),by=3.5,
     canvas = FALSE)

```


## 

```{r}
draw(dot(pos.x = -25, pos.y = 15, size.x = 2, size.y = 2))

for (i in seq(-25, 25, by = 4)) {
  for (j in seq(15, -15, by = -4)) {
    draw(dot(pos.x = i, pos.y = j, size.x = 2, size.y = 2), canvas = F)
  }
}

draw(rectangle(s.x=7,s.y=5,shd="white",pos.x=+15,pos.y=-10),
     canvas = FALSE)
```

```{r}
par(mfrow =c(2, 3), mar = c(0.5, 6, 0.5, 2) + .1, 
          mai=c(.1,.1,.1,.1),oma=c(4,4,0.2,0.2) )

container = rectangle(s.x=7,s.y=5,shd="white")

draw(container,xlim = 8)
clip(7,-7,5,-5)
for (i in seq(-22.5, 25, by = 4)) {
  for (j in seq(12.5, -15, by = -4)) {
    draw(dot(pos.x = i, pos.y = j, size.x = 2, size.y = 2), canvas = F)
  }
}

draw(container,xlim = 8)
clip(7,-7,5,-5)
draw(rectangle(s.x=7,s.y=5,shd="white",pos.x=0,pos.y=0),
     canvas = FALSE)
for (i in seq(-25, 25, by = 4)) {
  for (j in seq(15, -15, by = -4)) {
    draw(dot(pos.x = i, pos.y = j, size.x = 2, size.y = 2), canvas = F)
  }
}


draw(container,xlim = 8)

draw(container,xlim = 8)
draws(rectangle(s.x=7,s.y=5,shd="line.12.inv"),by=3.5,
     canvas = FALSE)

draw(container,xlim = 8)
draws(rectangle(s.x=7,s.y=5,shd="line.12.inv"),by=3.5,
     canvas = FALSE)
draws(rectangle(s.x=7,s.y=5,shd="line.12"),by=3.5,
     canvas = FALSE)

```



```{r}
draw(dot(pos.x = -25, pos.y = 15, size.x = 1, size.y = 1))

for (i in seq(-25, 25, by = 4)) {
  for (j in seq(15, -15, by = -4)) {
    draw(dot(pos.x = i, pos.y = j, size.x = 1, size.y = 1), canvas = F)
  }
}

draw(rectangle(s.x=7,s.y=5,shd="white",pos.x=+15,pos.y=-10),
     canvas = FALSE)
```

```{r}
par(mfrow =c(2, 3), mar = c(0.5, 6, 0.5, 2) + .1, 
          mai=c(.1,.1,.1,.1),oma=c(4,4,0.2,0.2) )

container = rectangle(s.x=7,s.y=5,shd="white")

draw(container,xlim = 8)
clip(7,-7,5,-5)
for (i in seq(-21, 25, by = 4)) {
  for (j in seq(12.5, -15, by = -4)) {
     draw(dot(pos.x = i, pos.y = j, size.x = 1, size.y = 1), canvas = F)
  }
}

draw(container,xlim = 8)
clip(7,-7,5,-5)
draw(rectangle(s.x=7,s.y=5,shd="white",pos.x=0,pos.y=0),
     canvas = FALSE)
for (i in seq(-25, 25, by = 4)) {
  for (j in seq(15, -15, by = -4)) {
        draw(dot(pos.x = i, pos.y = j, size.x = 1, size.y = 1), canvas = F)
  }
}


draw(container,xlim = 8)

draw(container,xlim = 8)
draws(rectangle(s.x=7,s.y=5,shd="line.12.inv"),by=3.5,
     canvas = FALSE)

draw(container,xlim = 8)
draws(rectangle(s.x=7,s.y=5,shd="line.12.inv"),by=3.5,
     canvas = FALSE)
draws(rectangle(s.x=7,s.y=5,shd="line.12"),by=3.5,
     canvas = FALSE)

```


```{r}

u.biscuit$pos.x[[1]] = -25
u.biscuit$pos.y[[1]] = 15


draw(size(u.biscuit, 4))

for (i in seq(-25, 25, by = 4)) {
  for (j in seq(15, -15, by = -4)) {
    u.biscuit$pos.x[[1]] = i
u.biscuit$pos.y[[1]] = j 
    
   
draw(size(u.biscuit, 4), canvas = F)
  }
}

draw(rectangle(s.x=7,s.y=5,shd="white",pos.x=+15,pos.y=-10),
     canvas = FALSE)
```

```{r}
par(mfrow =c(2, 3), mar = c(0.5, 6, 0.5, 2) + .1, 
          mai=c(.1,.1,.1,.1),oma=c(4,4,0.2,0.2) )

container = rectangle(s.x=7,s.y=5,shd="white")

draw(container,xlim = 8)
clip(7,-7,5,-5)
for (i in seq(-21, 25, by = 4)) {
  for (j in seq(12.5, -15, by = -4)) {
       u.biscuit$pos.x[[1]] = i
u.biscuit$pos.y[[1]] = j 
    
   
draw(size(u.biscuit, 4), canvas = F)
  }
}

draw(container,xlim = 8)
clip(7,-7,5,-5)
draw(rectangle(s.x=7,s.y=5,shd="white",pos.x=0,pos.y=0),
     canvas = FALSE)
for (i in seq(-25, 25, by = 4)) {
  for (j in seq(15, -15, by = -4)) {
        draw(dot(pos.x = i, pos.y = j, size.x = 1, size.y = 1), canvas = F)
  }
}


draw(container,xlim = 8)

draw(container,xlim = 8)
draws(rectangle(s.x=7,s.y=5,shd="line.12.inv"),by=3.5,
     canvas = FALSE)

draw(container,xlim = 8)
draws(rectangle(s.x=7,s.y=5,shd="line.12.inv"),by=3.5,
     canvas = FALSE)
draws(rectangle(s.x=7,s.y=5,shd="line.12"),by=3.5,
     canvas = FALSE)

```

```{r}

u.biscuit$pos.x[[1]] = -25
u.biscuit$pos.y[[1]] = 15


draw(size(u.biscuit, 4))

for (i in seq(-25, 25, by = 5)) {
  for (j in seq(15, -15, by = -5)) {
    u.biscuit$pos.x[[1]] = i
u.biscuit$pos.y[[1]] = j 
    
   
draw(size(u.biscuit, 4), canvas = F)
  }
}

draw(rectangle(s.x=7,s.y=5,shd="white",pos.x=+15,pos.y=-10),
     canvas = FALSE)
```

```{r}
par(mfrow =c(2, 3), mar = c(0.5, 6, 0.5, 2) + .1, 
          mai=c(.1,.1,.1,.1),oma=c(4,4,0.2,0.2) )

container = rectangle(s.x=7,s.y=5,shd="white")

draw(container,xlim = 8)
clip(7,-7,5,-5)
for (i in seq(-25, 25, by = 5)) {
  for (j in seq(15, -15, by = -5)) {
       u.biscuit$pos.x[[1]] = i
u.biscuit$pos.y[[1]] = j 
    
   
draw(size(u.biscuit, 4), canvas = F)
  }
}

draw(container,xlim = 8)
clip(7,-7,5,-5)
draw(rectangle(s.x=7,s.y=5,shd="white",pos.x=0,pos.y=0),
     canvas = FALSE)
for (i in seq(-25, 25, by = 5)) {
  for (j in seq(15, -15, by = -5)) {
        draw(dot(pos.x = i, pos.y = j, size.x = 1, size.y = 1), canvas = F)
  }
}


draw(container,xlim = 8)

draw(container,xlim = 8)
draws(rectangle(s.x=7,s.y=5,shd="line.12.inv"),by=3.5,
     canvas = FALSE)

draw(container,xlim = 8)
draws(rectangle(s.x=7,s.y=5,shd="line.12.inv"),by=3.5,
     canvas = FALSE)
draws(rectangle(s.x=7,s.y=5,shd="line.12"),by=3.5,
     canvas = FALSE)

```
:::


## Righe "complesse" verticali

::: panel-tabset
## Vertical Inner

```{r}
draw(vline(pos.x=-50, s.x = 30, lwd = 1), bg = "white")
for(i in seq(-30, 30, by = 1)) {
  
    draw(vline(pos.x = i, s.x=40, lwd = 1+ abs(i)/2), 
         canvas = F,  bg = "white")
}

draw(rectangle(s.x=7,s.y=5,shd="white",pos.x=+15,pos.y=-10),
     canvas = FALSE)
```


```{r}
par(mfrow =c(2, 3), mar = c(0.5, 6, 0.5, 2) + .1, 
          mai=c(.1,.1,.1,.1),oma=c(4,4,0.2,0.2) )

container = rectangle(s.x=7,s.y=5,shd="white")

draw(container,xlim = 8)
clip(7,-7,5,-5)
for(i in seq(14, 30, by = 1)) {
  
    draw(vline(pos.x = i-22, s.x=40, lwd = 1+ abs(i)/2), 
         canvas = F,  bg = "white")
}


draw(container,xlim = 8)
clip(7,-7,5,-5)
for(i in seq(14, 30, by = 1)) {
  
    draw(vline(pos.x = i-22, s.x=40, lwd = 5), 
         canvas = F,  bg = "white")
}



draw(container,xlim = 8)
draws(rectangle(s.x=7,s.y=5,shd="line.12.inv"),by=3.5,
     canvas = FALSE)

draw(container,xlim = 8)
draws(rectangle(s.x=7,s.y=5,shd="line.12.inv"),by=3.5,
     canvas = FALSE)
draws(rectangle(s.x=7,s.y=5,shd="line.12"),by=3.5,
     canvas = FALSE)

```

## Vertical Outer

```{r}
# vertical outer canvas
draw(vline(pos.x=-50, s.x = 30, lwd = 1), bg = "white")
p = seq(-30, 30, by = 2)
q = c(1, abs(p[(length(p)/2):1]), abs(p[1:(length(p)/2)]))
for(i in 1:length(p)) {
  draw(vline(pos.x = p[i], s.x=40, lwd = q[i]), 
       canvas = F,  bg = "white")
}
draw(rectangle(s.x=7,s.y=5,shd="white",pos.x=+15,pos.y=-10),
     canvas = FALSE)
```

## Vertical increasing

```{r}
draw(vline(pos.x=-50, s.x = 30, lwd = 1), bg = "white")
for(i in seq(-30, 30, by = 4)) {
  draw(vline(pos.x = i, s.x=40, lwd = 31 + i), 
       canvas = F,  bg = "white")
}

draw(rectangle(s.x=7,s.y=5,shd="white",pos.x=+15,pos.y=-10),
     canvas = FALSE)

```

## Vertical decreasing

```{r}

draw(vline(pos.x=-50, s.x = 30, lwd = 1), bg = "white")
for(i in seq(-30, 30, by = 4)) {
  draw(vline(pos.x = i, s.x= 40, lwd = 31 - i), 
       canvas = F,  bg = "white")
}

draw(rectangle(s.x=7,s.y=5,shd="white",pos.x=+15,pos.y=-10),
     canvas = FALSE)

```
:::

# Matrici $2 \times 2$

## Rotazione Diagonale

```{r}
m1 = apply(
  Raven(
    st1 = square(), 
    vrule = "rotation", 
    hrule = "rotation"
  )
)
draw(m1, n.cell = 4)


```

```{r}
dist_m1 = responses(m1)
draw.dist(dist_m1, n.resp = 11, main = T)
```

## Rotazione Verticale

```{r}
m2 = apply(
  Raven(
    st1 = square(), 
    vrule = "rotation", 
    hrule = "identity"
  )
)
draw(m2, n.cell = 4)
```

```{r}
dist_m2 = responses(m2)
draw.dist(dist_m2, n.resp = 11, main = T)
```

## Forma e dimensione Verticale

Ci sono problemi, perché qui bisogna mettere ben 3 forme e prende come corretta la forma che non è visibile

```{r}
m3 = apply(
  Raven(
    st1 = cof(pacman(), 
              square(), 
              ellipse()), 
    vrule = c("diff_shapes", "size"), 
    hrule = "identity"
  )
)
draw(m3, n.cell = 9, hide = F)
```



```{r}
dist_m3 = responses(m3, mat.type = 4)

draw.dist(dist_m3, n.resp = 11, main = T)
```

## Verticale e Orizzontale

```{r}
m4 = apply(
  Raven(
    st1 = cof(pacman(), 
              square(), 
              ellipse()), 
    vrule = c("diff_shapes"), 
    hrule = "size"
  )
)
draw(m4, n.cell = 4)
```


```{r}
dist_m4 = responses(m4, mat.type = 4)

draw.dist(dist_m4)
```


:::

## Forma e riempimento

::: panel-tabset
## Verticale

```{r}

m5 = apply(
  Raven(
    st1 = cof(e.hexagon(), 
              ellipse(), ellipse()), 
    vrule = c("diff_shapes", "filline"), 
    hrule = "identity"
  )
)
draw(m5, n.cell = 4)

```

## Non stampa i distarttori nel pdf ma non capisco come mai

```{r}
dist_m5 = responses(m5, mat.type = 4)

draw.dist(dist_m5)

```



## Verticale e orizzontale

```{r}
m6 = apply(
  Raven(
    st1 = cof(e.hexagon(), 
              square(), ellipse()), 
    vrule = c("diff_shapes"), 
    hrule =  "filline"
  )
)
draw(m6, n.cell = 4)

```
:::


```{r}
dist_m6 = responses(m6, mat.type = 4)
draw.dist(dist_m6)
```


## Forma e orientamento Verticale

```{r}
m7 = apply(
  Raven(
    st1 = cof(slice(s.x = 15), 
              square(), pacman()), 
    vrule = c("diff_shapes", "rotation"), 
    hrule =  "identity"
  )
)
draw(m7, n.cell = 4)
```

```{r}
dist_m7 = responses(m7)

draw.dist(dist_m7)

```


## Forma e orientamento Verticale e orizzontale

```{r}
m8 = apply(
  Raven(
    st1 = cof(ellipse(), 
              pentagon(), ellipse()), 
    vrule = c("diff_shapes"), 
    hrule =  "rotation"
  )
)
draw(m8, n.cell = 4)
```

```{r}
dist_m8 = responses(m8, mat.type = 4)

draw.dist(dist_m8)
```

:::

## Forma e bordo Verticale

```{r}
m8_bis = apply(
  Raven(
    st1 = cof(circle(), 
              square(), pacman()), 
    vrule = c("diff_shapes", "lty"), 
    hrule =  "identity"
  )
)
draw(m8_bis, n.cell = 4)

```

```{r}
dist_m8bis = responses(m8_bis, mat.type = 4)

draw.dist(dist_m8bis)

```


##  Forma e bordo Verticale e orizzontale

```{r}
m9 = apply(
  Raven(
    st1 = cof(pacman(), 
              u.bow.tie(), pacman()), 
    vrule = c("diff_shapes"), 
    hrule =  "lty"
  )
)
draw(m9, n.cell = 4)
```
:::


```{r}
dist_m9 = responses(m9, mat.type = 4)

draw.dist(dist_m9)

```


# Matrici $3 \times 3$

\newpage 

## Forma e dimensione  Verticale

```{r}
set.seed(999)
M1 = apply(
  Raven(
    st1 = cof(square(shd = "black"), u.star(), triangle(rot = pi/2, shd = "black")), 
    vrule = c("diff_shapes", "size"), 
    hrule = "identity"
  )
)

draw(M1, n.cell = 9, hide = T)


```

```{r}

dist_m1 = responses(M1)
draw.dist(dist_m1, n.resp = 11, main = T)

```

## Gemella 1

```{r}

e_VertFill_HorLi_01a = apply(
  Raven(
    st1 = pacman(), 
    vrule = c("fill"), 
    hrule = "identity"
  )
)


e_VertFill_HorLi_01b = apply(
  Raven(
    st1 = square(s.x = 18, s.y = 18), 
    vrule = c("identity"), 
    hrule = c("lty", "lwd")
  )
)

elisa001 = com(e_VertFill_HorLi_01a, e_VertFill_HorLi_01b)
draw(elisa001, hide = T)
```


```{r}
dist_e001 = responses(elisa001)

draw.dist(dist_e001, n.resp = 11)

```



## Gemella 2 


```{r}
m_VertShSi_01= apply(
  Raven(
    st1 = cof(pentagon(shd = "black"), triangle(shd = "black"), square(shd = "black")), 
    vrule = c("diff_shapes", "size"), 
    hrule = "identity"
  )
)

draw(m_VertShSi_01, hide = T)
```

```{r}
dist_m001 = responses(m_VertShSi_01)
draw.dist(dist_m001, n.resp = 11, main = T)
```


## Forma e dimensione Verticale e orizzontale

```{r}
M2 = apply(
  Raven(
    st1 = cof(square(shd = "black"), u.star(), triangle(rot = pi/2, shd = "black")), 
    vrule = c("diff_shapes"), 
    hrule = "size"
  )
)
draw(M2, n.cell = 9, hide = T)
```

```{r}
dist_M2 = responses(M2)

draw.dist(dist_M2)
```

## Gemella 1 

```{r}
e_TLLRLi_VertFill_01a = apply(
  Raven(
    st1 = square(s.x = 20, s.y = 20), 
    vrule = c("lty", "lwd"), 
    hrule = c("lty", "lwd")
  )
)

e_TLLRLi_VertFill_01b = apply(
  Raven(
    st1 = triangle(s.x = 10, s.y = 10), 
    vrule = c("fill"), 
    hrule = c("identity")
  )
)

elisa002 = com(com(e_TLLRLi_VertFill_01a, e_TLLRLi_VertFill_01b))
draw(elisa002)

```



```{r}
dist_e002 = responses(elisa002)

draw.dist(dist_e002)

```


:::

## Gemella 2

```{r}
m_VertShSi_01= apply(
  Raven(
    st1 = cof(pentagon(shd = "black"), triangle(shd = "black"), square(shd = "black")), 
    vrule = c("diff_shapes", "size"), 
    hrule = "identity"
  )
)
draw(m_VertShSi_01)
```

```{r}
dist_m002 = responses(m_VertShSi_01)

draw.dist(dist_m002)
```





## Forma e rimepimento Verticale

```{r}
M3a = apply(
  Raven(
    st1 = cof(square(), 
              circle(s.x = 15, s.y = 15), pentagon()), 
    vrule = c("diff_shapes"), 
    hrule = "identity"
  )
)


M3b = apply(
  Raven(
    st1 = cof(s.lilth, 
              luck(s.x = 5, s.y = 7), 
              u.papillon), 
    vrule = c("diff_shapes"), 
    hrule = "identity"
  )
)

M3 = com(M3a, M3b)
draw(M3)




```

```{r}
dist_M3 = responses(M3)


draw.dist(dist_M3)


```

## Gemella 

```{r}
e_TLLRFill_TRLLLi_01a = apply(
  Raven(
    st1 = circle(s.x = 17, s.y = 17), 
    vrule = c("lty", "lwd"), 
    hrule = c("lty", "lwd")
  )
)

e_TLLRFill_TRLLLi_01b = apply(
  Raven(
    st1 = cof(u.star(), 
              u.bow.tie(), 
              triangle(s.x = 14, s.y=14, rot = pi/2, shd = "black", pos.y = -1)), 
    vrule = c("diff_shapes"), 
    hrule = c("diff_shapes")
  )
)

elisa003 = com(e_TLLRFill_TRLLLi_01a, e_TLLRFill_TRLLLi_01b)
draw(elisa003)
```

```{r}
dist_e003 = responses(elisa003)

draw.dist(dist_e003)
```


## Gemella 2

```{r}
m_VertSh_HorSi_01  = apply(
  Raven(
    st1 = cof(luck(), ellipse(), slice()), 
    vrule = c("diff_shapes"), 
    hrule = "size"
  )
)
draw(m_VertSh_HorSi_01)
```


```{r}
dist_m003 = responses(m_VertSh_HorSi_01)

draw.dist(dist_m003)

```


## Forma e rimepimento Verticale e orizzontale

```{r}
M4a = apply(
  Raven(
    st1 = cof(square(), 
              circle(s.x=15, s.y=15), e.hexagon()), 
    vrule = c("diff_shapes"), 
    hrule = "identity"
  )
)

M4b = apply(
  Raven(
    st1 = cof(s.lilth, 
              luck(s.x = 5, s.y = 7), 
              u.papillon), 
    vrule = c("identity"), 
    hrule = "diff_shapes"
  )
)
M4 = com(M4a, M4b)
draw(M4, n.cell=9)
```

```{r}
dist_M4 = responses(M4)

draw.dist(dist_M4)

```

## Gemella 

```{r}
e_VertShFillLi_01a = apply(
  Raven(
    st1 = cof(triangle(s.x = 17, s.y = 17), 
              rot.hexagon(shd = "white"), 
              luck(s.x = 15, s.y = 20)), 
    vrule = c("diff_shapes", "lty", "lwd"), 
    hrule = c("identity")
  )
)

e_VertShFillLi_01b = apply(
  Raven(
    st1 = cof(circle(s.x = 5, s.y = 5), 
              u.pie.2(), 
              e.hexagon(s.x = 6, s.y = 6)), 
    vrule = c("diff_shapes"), 
    hrule = c("identity")
  )
)
elisa004 = com(e_VertShFillLi_01a, e_VertShFillLi_01b)

draw(elisa004)
```


```{r}
dist_e004 = responses(elisa004)

draw.dist(dist_e004)
```


## Gemella 2 

```{r}
m_VertShFil_01a = apply(
  Raven(
    st1 = cof(square(s.x = 18,s.y = 18), 
              triangle(s.x = 16, s.y = 16,rot = pi/2), pentagon(s.x = 16,s.y = 16)), 
    vrule = c("diff_shapes"), 
    hrule = "identity"
  )
)

m_VertShFil_01b = apply(
  Raven(
    st1 = cof(pacman(), 
              s.lilth, 
              u.papillon), 
    vrule = c("diff_shapes"), 
    hrule = "identity"
  )
)

max_004 = com(m_VertShFil_01a, m_VertShFil_01b)
draw(max_004)

```

```{r}
dist_m004 = responses(max_004)

draw.dist(dist_m004)
```


## TL-LR per la prima regola, V per la seconda

```{r}
M5a = apply(
  Raven(
    st1 = cof(square(), 
              ellipse(), e.hexagon()), 
    vrule = c("diff_shapes"), 
    hrule = "diff_shapes"
  )
)

M5b = apply(
  Raven(
    st1 = cof(s.lilth, 
              luck(s.x = 5, s.y = 7), 
              u.thepie), 
    vrule = c("diff_shapes"), 
    hrule = "identity"
  )
)

M5 = com(M5a, M5b)

draw(M5, n.cell=9)
```


```{r}
dist_M5 = responses(M5)

draw.dist(dist_M5)

```


## Gemella 1
```{r}
e_HorShFill_VertLi_01a = apply(
  Raven(
    st1 = cof(circle(s.x = 15, s.y = 15), 
              pentagon(), 
              square(s.x = 18, s.y = 18)), 
    vrule = c("diff_shapes", "lty", "lwd"), 
    hrule = c("identity")
  )
)

e_HorShFill_VertLi_01b = apply(
  Raven(
    st1 = cof(X(), 
              triangle(s.x = 7, s.y = 7), 
              u.pie.2.inv()), 
    vrule = c("identity"), 
    hrule = c("diff_shapes")
  )
)
elisa_005 = com(e_HorShFill_VertLi_01a, e_HorShFill_VertLi_01b)

draw(elisa_005)



```

```{r}
dist_e005 = responses(elisa_005)

draw.dist(dist_e005)

```


## Gemella 2 


```{r}
m_VertSh_HorFil_01a = apply(
  Raven(
    st1 = cof(square(), 
              circle(), e.hexagon()), 
    vrule = c("diff_shapes"), 
    hrule = "identity"
  )
)


m_VertSh_HorFil_01b = apply(
  Raven(
    st1 = cof(cross(), 
              dot(), 
               luck(s.x = 5,s.y = 5)), 
    vrule = c("identity"), 
    hrule = "diff_shapes"
  )
)
max_005 = com(m_VertSh_HorFil_01a, m_VertSh_HorFil_01b)
draw(max_005)
```

```{r}
dist_m005 = responses(max_005)

draw.dist(dist_m005)

```


## TL-LR per la prima, TR-LL per la seconda

```{r}
M6a = apply(
  Raven(
    st1 = cof(square(), 
              circle(s.x = 15, s.y = 15), pentagon()), 
    vrule = c("diff_shapes"), 
    hrule = "diff_shapes"
  )
)

M6b = apply(
  Raven(
    st1 = cof(u.biscuit, 
              luck(s.x = 5, s.y = 7, shd = "black"), 
              triangle(s.x = 8, s.y=8, rot = pi/2, shd = "black", pos.y = -1)), 
    vrule = c("diff_shapes.inv"), 
    hrule = "diff_shapes.inv"
  )
)

M6 = com(M6a, M6b)

draw(M6, n.cell=9)
```
:::

```{r}
dist_M6 = responses(M6)

draw.dist(dist_M6)

```


## Gemella 1 

```{r}
e_TLLRSh_VertLil_HorFill_01a = apply(
  Raven(
    st1 = cof(circle(), 
              u.pie.2(), 
              u.pie.4()), 
    vrule = c("diff_shapes", "lty", "lwd"), 
    hrule = c("diff_shapes")
  )
)

e_TLLRSh_VertLil_HorFill_01b = apply(
  Raven(
    st1 = pentagon(s.x = 3, s.y = 3), 
    vrule = c("identity"), 
    hrule = "fill"
  )
)

elisa006 = com(e_TLLRSh_VertLil_HorFill_01a, e_TLLRSh_VertLil_HorFill_01b)

draw(elisa006)


```



```{r}
dist_e006 = responses(elisa006)

draw.dist(dist_e006)

```

## Gemella 2 

```{r}
m_TLLRSh_VertFil_01a = apply(
  Raven(
    st1 = cof(circle(s.x = 15,s.y=15), 
              pentagon(), e.hexagon()), 
    vrule = c("diff_shapes"), 
    hrule = "diff_shapes"
  )
)

m_TLLRSh_VertFil_01b = apply(
  Raven(
    st1 = cof(s.lilth, 
              triangle(s.x = 5, s.y = 5), 
              u.papillon), 
    vrule = c("diff_shapes"), 
    hrule = "identity"
  )
)
m_006 = com(m_TLLRSh_VertFil_01a, m_TLLRSh_VertFil_01b)
draw(m_006)
```


```{r}
dist_m006 = responses(m_006)

draw.dist(dist_m006)
```


## Forma e orientamento Verticale

```{r}
M7 = apply(
  Raven(
    st1 = cof(luck(), 
              ellipse(rot=pi/2), pentagon()), 
    vrule = c("diff_shapes", "rotation"), 
    hrule = "identity"
  )
)
draw(M7, n.cell = 9)
```


```{r}
dist_M7 = responses(M7)

draw.dist(dist_M7)
```

## Gemella 

```{r}
e_TLLRSh_TRLLFillLi_01a = apply(
  Raven(
    st1 = cof(ellipse(s.x = 15, s.y = 11), 
              e.hexagon(), 
              square()), 
    vrule = c("diff_shapes.inv", "lty", "lwd"), 
    hrule = c("diff_shapes.inv", "lty", "lwd")
  )
)


e_TLLRSh_TRLLFillLi_01b = apply(
  Raven(
    st1 =  cof(dot(), 
               s.lily(), 
               vline()), 
    vrule = c("diff_shapes"), 
    hrule = "diff_shapes"
  )
)

elisa007 = com(e_TLLRSh_TRLLFillLi_01b, e_TLLRSh_TRLLFillLi_01a)


draw(elisa007)
```


```{r}
dist_e007 = responses(elisa007)

draw.dist(dist_e007)

```

## Gemella 2

```{r}
m_TLLRSh_TRLLFil_01a = apply(
  Raven(
    st1 = cof(e.hexagon(s.x = 17,s.y = 17), 
              luck(s.x = 17, s.y = 20), pentagon(s.x = 17,s.y = 17)), 
    vrule = c("diff_shapes"), 
    hrule = "diff_shapes"
  )
)

m_TLLRSh_TRLLFil_01b = apply(
  Raven(
    st1 = cof(pacman(), 
              square(s.x = 8, s.y = 8), 
              s.lily()), 
    vrule = c("diff_shapes.inv"), 
    hrule = "diff_shapes.inv"
  )
)

m_007 = com(m_TLLRSh_TRLLFil_01a, m_TLLRSh_TRLLFil_01b)

draw(m_007)
```

```{r}
dist_m007 = responses(m_007)

draw.dist(dist_m007)

```



## Verticale e orizzontale

```{r}

M8 = apply(
  Raven(
    st1 = cof(luck(), 
              ellipse(rot=pi/2), pentagon()), 
    vrule = c("diff_shapes"), 
    hrule = "rotation"
  )
)
draw(M8, n.cell = 9)
```


```{r out.width="80%"}
dist_M8 = responses(M8)

draw.dist(dist_M8)

```

## Gemella 


```{r}


e_VertShSi_HorFill_01 = apply(
  Raven(
    st1 = cof(e.hexagon(), 
              pentagon(), 
              triangle()), 
    vrule = c("diff_shapes", "size"), 
    hrule = c("parfill")
  )
)
elisa008 = e_VertShSi_HorFill_01
draw(elisa008, n.cell = 9)

```


```{r}
dist_e008 = responses(elisa008)

draw.dist(dist_e008)


```


## Gemella 2

```{r}
m_VertShOr_01 = apply(
  Raven(
    st1 = cof(triangle(), 
              square(), pentagon()), 
    vrule = c("diff_shapes", "rotation"), 
    hrule = "identity"
  )
)
draw(m_VertShOr_01)
```


```{r}
dist_m008 = responses(m_VertShOr_01)

draw.dist(dist_m008)
```


## TL-LR sulla prima, verticale sulla seconda

```{r}
M10 = apply(
  Raven(
    st1 = cof(luck(), 
              pacman(), pentagon()), 
    vrule = c("diff_shapes", "rotation"), 
    hrule = "diff_shapes"
  )
)
draw(M10, n.cell = 9)
```

```{r}
dist_M10 = responses(M10)

draw.dist(dist_M10)

```

## Gemella 

```{r}
e_TLLRSh_VertFillSi_01a = apply(
  Raven(
    st1 = cof(triangle(s.x=16, s.y=16), 
              e.hexagon(), 
              circle(s.x=17, s.y = 14)), 
    vrule = c("diff_shapes"), 
    hrule = c("diff_shapes")
  )
)

elisa010 = e_TLLRSh_VertFillSi_01a
draw(elisa010)
```

```{r}
dist_e010 = responses(elisa010)

draw.dist(dist_e010)

```

## Gemella 2

```{r}
m_VertSh_HorOr_01 = apply(
  Raven(
    st1 = cof(triangle(), 
              slice(), pentagon()), 
    vrule = c("diff_shapes"), 
    hrule = "rotation"
  )
)
draw(m_VertSh_HorOr_01)

```


```{r}
dist_m009 = responses(m_VertSh_HorOr_01)

draw.dist(dist_m009)

```

## TR-LL sulla prima, TL-LR sulla seconda

```{r}
M11 = apply(
  Raven(
    st1 = cof(luck(), 
              pacman(), e.hexagon()), 
    vrule = c("diff_shapes.inv", "rotation"), 
    hrule = c("diff_shapes.inv", "rotation")
  )
)
draw(M11, n.cell = 9)
```
:::

```{r}
dist_M11 = responses(M11)

draw.dist(dist_M11)
```

## gemella 

```{r}
m_TLLRSh_VertOr_01 = apply(
  Raven(
    st1 = cof(e.hexagon(rot=pi/2), 
              pacman(), triangle()), 
    vrule = c("diff_shapes", "rotation"), 
    hrule = "diff_shapes"
  ))
draw(m_TLLRSh_VertOr_01)
```


```{r}
dist_m010 = responses(m_TLLRSh_VertOr_01)

draw.dist(dist_m010)

```


## Forma e bordo Verticale

```{r}
M11 = apply(
  Raven(
    st1 = cof(luck(), 
              pacman(), e.hexagon()), 
    vrule = c("diff_shapes", "lty", "lwd"), 
    hrule = "identity"
  )
)
draw(M11, n.cell = 9)

```

```{r}
dist_M11 = responses(M11)

draw.dist(dist_M11)

```




## Gemella 

```{r}
e_TLLRSh_VertFillSi_01b = apply(
  Raven(
    st1 = cof(square(s.x=6, s.y=6)), 
    vrule = c("fill", "size"), 
    hrule = c("identity")
  )
)
elisa011 = com(e_TLLRSh_VertFillSi_01a, e_TLLRSh_VertFillSi_01b)
draw(elisa011)

```


```{r}
dist_e011 = responses(elisa011)

draw.dist(dist_e011)

```


## Gemella 2 

```{r}
m_TLLRSh_TRLLOr_01 = apply(
  Raven(
    st1 = cof(luck(), 
              slice(), pentagon()), 
    vrule = c("diff_shapes.inv", "rotation"), 
    hrule = c("diff_shapes.inv", "rotation")
  )
)
draw(m_TLLRSh_TRLLOr_01)
```

```{r}
dist_m011 = responses(m_TLLRSh_TRLLOr_01)

draw.dist(dist_m011)

```


## Verticale e orizzontale

```{r}
M12 = apply(
  Raven(
    st1 = cof(s.lily(), 
              u.bow.tie(), triangle()), 
    vrule = c("diff_shapes"), 
    hrule = c("lty", "lwd")
  )
)
draw(M12, n.cell = 9)
```

```{r}
dist_M12 = responses(M12)

draw.dist(dist_M12)

```


## Gemella 

```{r}
e_TLLRSh_TRLLFillSi_01a = apply(
  Raven(
    st1 = cof(circle(s.x = 13, s.y=13, shd = "black"), 
              u.star(), 
              square(s.x = 15, s.y=15, shd = "black")), 
    vrule = c("diff_shapes.inv"), 
    hrule = c("diff_shapes.inv")
  )
)
elisa012 = e_TLLRSh_TRLLFillSi_01a
draw(elisa012)
```


```{r}
dist_e012 = responses(elisa012)
draw.dist(dist_e012)
```

## Gemella 2

```{r}
m_VertShLi_01= apply(
  Raven(
    st1 = cof(triangle(), 
              square(), pentagon()), 
    vrule = c("diff_shapes", "lty", "lwd"), 
    hrule = "identity"
  )
)
draw(m_VertShLi_01)
```


```{r}
dist_m012 = responses(m_VertShLi_01)

draw.dist(dist_m012)

```


## TL-LR sulla prima, V sulla seconda

```{r}
M13 = apply(
  Raven(
    st1 = cof(s.lily(), 
              u.bow.tie(), pacman()), 
    vrule = c("diff_shapes"), 
    hrule = c("diff_shapes", "lty", "lwd")
  )
)
draw(M13, n.cell = 9)


```


```{r}
dist_M13 = responses(M13)

draw.dist(dist_M13)
```


## Gemella 

```{r}
e_TLLRSh_TRLLFillSi_01b = apply(
  Raven(
    st1 = cof(triangle(s.x=15, s.y=15, shd = "white"), 
              circle(s.x=6, s.y=6, shd = "white"), 
              e.hexagon(s.x=10, s.y=10, shd="white")), 
    vrule = c("diff_shapes", "size"), 
    hrule = c("diff_shapes", "size")
  )
)

elisa013 = com(e_TLLRSh_TRLLFillSi_01a, e_TLLRSh_TRLLFillSi_01b)

draw(elisa013)

```


```{r}
dist_e013 = responses(elisa013)

draw.dist(dist_e013)

```


## Gemella 2

```{r}
m_VertSh_HorLi_01 = apply(
  Raven(
    st1 = cof(ellipse(), 
              pacman(), u.pie.4()), 
    vrule = c("diff_shapes"), 
    hrule = c("lty", "lwd")
  )
)
draw(m_VertSh_HorLi_01)
```



```{r}
dist_m012 = responses(m_VertSh_HorLi_01)

draw.dist(dist_m012)

```


## TL-LR sulla prima, TR-LL sulla seconda

```{r}
M14 = apply(
  Raven(
    st1 = cof(s.lily(), 
              u.bow.tie(), pacman()), 
    vrule = c("diff_shapes", "lty", "lwd"), 
    hrule = c("diff_shapes", "lty", "lwd")
  )
)
draw(M14, n.cell = 9)
```
:::


```{r}
dist_M14 = responses(M14)

draw.dist(dist_M14)

```


## Gemella 

```{r}
m_TLLRSh_VertLi_01 = apply(
  Raven(
    st1 = cof(e.hexagon(), 
              circle(), luck()), 
    vrule = c("diff_shapes"), 
    hrule = c("diff_shapes", "lty", "lwd")
  )
)
draw(m_TLLRSh_VertLi_01)
```


```{r}
dist_m013 = responses(m_TLLRSh_VertLi_01)

draw.dist(dist_m013)

```


## Rimepimento e orientamento Verticale

```{r}
M15a = apply(
  Raven(
    st1 = pentagon(), 
    vrule = c("rotation"), 
    hrule = c("identity")
  )
)



M15b = apply(
  Raven(
    st1 = cof(u.biscuit, 
              luck(s.x = 5, s.y = 7, shd = "black"), 
              triangle(s.x = 8, s.y=8, rot = pi/2, shd = "black", pos.y = -1)), 
    vrule = c("diff_shapes.inv"), 
    hrule = "identity"
  )
)

M15 = com(M15a, M15b)
draw(M15, n.cell=9)
```


```{r}
dist_M15 = responses(M15)

draw.dist(dist_M15)

```


## Gemella 

```{r}
m_TLLRSh_TRLLLi_01 = apply(
  Raven(
    st1 = cof(u.pie.2(), 
              slice(), square()), 
    vrule = c("diff_shapes", "lty", "lwd"), 
    hrule = c("diff_shapes", "lty", "lwd")
  )
)
draw(m_TLLRSh_TRLLLi_01)
```


```{r}
dist_m014 = responses(m_TLLRSh_TRLLLi_01)


draw.dist(dist_m014)
```


## Vertical e orizzontale

```{r}
M16a = apply(
  Raven(
    st1 = e.hexagon(), 
    vrule = c("rotation"), 
    hrule = c("identity")
  )
)



M16b = apply(
  Raven(
    st1 = cof(s.lilth, 
              u.thepie, 
              u.papillon), 
    vrule = c("identity"), 
    hrule = "diff_shapes.inv"
  )
)

M16 = com(M16a, M16b)
draw(M16, n.cell=9)
```

```{r}
dist_M16 = responses(M16)

draw.dist(dist_M16)

```


## Gemella 

```{r}
m_VertFilOr_01a = apply(
  Raven(
    st1 = triangle(s.x = 15,s.y = 15), 
    vrule = c("rotation"), 
    hrule = c("identity")
  )
)


m_VertFilOr_01b = apply(
  Raven(
    st1 = cof(square(s.x = 5,s.y = 5,shd = "black"), 
              pentagon(s.x = 5, s.y = 5, shd = "black"), 
              circle(s.x = 5, s.y=5, shd = "black", pos.y = -1)), 
    vrule = c("diff_shapes.inv"), 
    hrule = "identity"
  )
)

m_015 = com(m_VertFilOr_01a, m_VertFilOr_01b)
draw(m_015)

```


```{r}
dist_m015 = responses(m_015)

draw.dist(dist_m015)

```


## TL-LR entrambe

```{r}
M17a = apply(
  Raven(
    st1 = pacman(), 
    vrule = c("rotation"), 
    hrule = c("rotation")
  )
)

M17b = apply(
  Raven(
    st1 = circle(s.x = 2, s.y = 2), 
    vrule = c("fill"), 
    hrule = "fill"
  )
)
M17 = com(M17a, M17b)

draw(M17, n.cell=9)
```
:::

```{r}
dist_M17 = responses(M17)

draw.dist(dist_M17)

```


## Gemella 

```{r}
m_VertFil_HorOr_01a = apply(
  Raven(
    st1 = pentagon(s.x = 17,s.y=17), 
    vrule = c("rotation"), 
    hrule = c("identity")
  )
)


m_VertFil_HorOr_01b = apply(
  Raven(
    st1 = cof(dot(), 
              pacman(), 
              slice(pos.y = -3)), 
    vrule = c("identity"), 
    hrule = "diff_shapes.inv"
  )
)
m_016 = com(m_VertFil_HorOr_01a, m_VertFil_HorOr_01b)
draw(m_016)
```


```{r}

dist_m016 = responses(m_016)

draw.dist(dist_m016)

```




## Riempimento e bordo Verticale

```{r}
M18a = apply(
  Raven(
    st1 = square(), 
    vrule = c("identity"), 
    hrule = c("identity")
  )
)



M18b = apply(
  Raven(
    st1 = circle(s.x = 3, s.y = 3), 
    vrule = c("fill"), 
    hrule = "identity"
  )
)


M18c = apply(
  Raven(
    st1 = square(s.x = 13, s.y = 13), 
    vrule = c("lty", "lwd"), 
    hrule = c("identity")
  )
)



M18 = com(M18a, M18b, M18c) 
draw(M18)
```


```{r}
dist_M18 = responses(M18)
draw.dist(dist_M18)

```


```{r}
m_TLLRFil_VertOr_01a = apply(
  Raven(
    st1 = e.hexagon(), 
    vrule = c("rotation"), 
    hrule = c("identity")
  )
)


m_TLLRFil_VertOr_01b = apply(
  Raven(
    st1 = square(s.x = 4, s.y = 4), 
    vrule = c("fill"), 
    hrule = "fill"
  )
)

m_017 = com(m_TLLRFil_VertOr_01a, m_TLLRFil_VertOr_01b)
draw(m_017)
```

```{r}
dist_m017 = responses(m_017)

draw.dist(dist_m017)
```


## Verticale e orizzontale

```{r}
M19a = apply(
  Raven(
    st1 = circle(s.x = 3, s.y = 3), 
    vrule = c("fill"), 
    hrule = "identity"
  )
)

M19b = apply(
  Raven(
    st1 = triangle(s.x = 13, s.y = 13), 
    vrule = c("identity"), 
    hrule = c("lty", "lwd")
  )
)
 M19 = com(M19a, M19b)

draw(M19)
```

```{r}
dist_M19 = responses(M19)

draw.dist(dist_M19)

```


## Gemella 

```{r}
m_TLLRFil_TRLLOr_01a = apply(
  Raven(
    st1 = triangle(), 
    vrule = c("rotation"), 
    hrule = c("rotation")
  )
)

m_TLLRFil_TRLLOr_01b = apply(
  Raven(
    st1 = triangle(s.x = 5, s.y = 5), 
    vrule = c("fill"), 
    hrule = "fill"
  )
)
m_018 = com(m_TLLRFil_TRLLOr_01a, m_TLLRFil_TRLLOr_01b)
draw(m_018)
```


```{r}
dist_m018 = responses(m_018)

draw.dist(dist_m018)

```


## TL-LR, Verticale

```{r}
M20a = apply(
  Raven(
    st1 = pacman(), 
    vrule = c("lty", "lwd"), 
    hrule = c("lty", "lwd")
  )
)


M20b = apply(
  Raven(
    st1 = square(s.x = 3, s.y = 3), 
    vrule = c("fill"), 
    hrule = c("identity")
  )
)

M20 = com(M20a, M20b)
draw(M20)
```

```{r}
dist_M20 = responses(M20)

draw.dist(dist_M20)

```


## TL-LR

```{r}
M21a = apply(
  Raven(
    st1 = e.hexagon(), 
    vrule = c("lty", "lwd"), 
    hrule = c("lty", "lwd")
  )
)

M21b = apply(
  Raven(
    st1 = cof(u.biscuit, 
              luck(s.x = 5, s.y = 7, shd = "black"), 
              triangle(s.x = 8, s.y=8, rot = pi/2, shd = "black", pos.y = -1)), 
    vrule = c("diff_shapes"), 
    hrule = c("diff_shapes")
  )
)
M21 = com(M21a, M21b)
draw(M21)

```
:::

```{r}
dist_M21 = responses(M21)
draw.dist(dist_M21)
```


## Forma riempimento bordo

::: panel-tabset
## Verticale

```{r}

M22a = apply(
  Raven(
    st1 = cof(square(), 
              pentagon(), 
              e.hexagon()), 
    vrule = c("diff_shapes", "lty", "lwd"), 
    hrule = c("identity")
  )
)
M22b = apply(
  Raven(
    st1 = cof(s.lilth, 
              luck(s.x = 5, s.y = 7), 
              u.thepie), 
    vrule = c("diff_shapes"), 
    hrule = c("identity")
  )
)
M22 = com(M22a, M22b)
draw(M22)
```

```{r}

dist_M22 = responses(M22)
draw.dist(dist_M22)

```


## Verticale e orizzontale

```{r}
M23a = apply(
  Raven(
    st1 = cof(square(), 
              pentagon(), 
              e.hexagon()), 
    vrule = c("diff_shapes", "lty", "lwd"), 
    hrule = c("identity")
  )
)

M23b = apply(
  Raven(
    st1 = cof(s.lilth, 
              u.papillon, 
              u.thepie), 
    vrule = c("identity"), 
    hrule = c("diff_shapes")
  )
)

M23 = com(M23a, M23b)
draw(M23)
```

```{r}
dist_M23 = responses(M23)

draw.dist(dist_M23)
```


## TL-LR, Verticale

```{r}
M24a = apply(
  Raven(
    st1 = cof(triangle(), 
              pacman(), 
              u.bow.tie()), 
    vrule = c("diff_shapes", "lty", "lwd"), 
    hrule = c("diff_shapes")
  )
)

M24b = apply(
  Raven(
    st1 = circle(s.x = 3, s.y = 3), 
    vrule = c("identity"), 
    hrule = "fill"
  )
)
M24 = com(M24a, M24b)

draw(M24)


```


```{r}
dist_M24 = responses(M24)

draw.dist(dist_M24)

```


## TL-LR, TR-LL

```{r}
M25a = apply(
  Raven(
    st1 = cof(triangle(), 
              e.hexagon(), 
              luck()), 
    vrule = c("diff_shapes.inv", "lty", "lwd"), 
    hrule = c("diff_shapes.inv", "lty", "lwd")
  )
)


u.biscuit1 = u.biscuit
u.biscuit1$size.x[[1]] = u.biscuit1$size.x[[1]]-2
u.biscuit1$size.y[[1]] = u.biscuit1$size.y[[1]]-2
M25b = apply(
  Raven(
    st1 =  cof(u.biscuit1, 
    luck(s.x = 4, s.y = 6, shd = "black"), 
    circle(s.x = 3, s.y = 3, shd="black")), 
    vrule = c("diff_shapes"), 
    hrule = "diff_shapes"
  )
)

M25 = com(M25b, M25a)
draw(M25)
```
:::

```{r}
dist_M25 = responses(M25)

draw.dist(dist_M25)

```


## Forma riempimento dimensione

::: panel-tabset
## Verticale

```{r}
M26 = apply(
  Raven(
    st1 = cof(square(), 
              pentagon(), 
              circle()), 
    vrule = c("diff_shapes", "size", "parfill"), 
    hrule = c("identity")
  )
)
draw(M26, n.cell = 9)
```


```{r}
dist_M26 = responses(M26)

draw.dist(dist_M26)
```

## Verticale e orizzontale

```{r}
M27 = apply(
  Raven(
    st1 = cof(luck(), 
              triangle(), 
              ellipse()), 
    vrule = c("diff_shapes", "size"), 
    hrule = c("parfill")
  )
)
draw(M27, n.cell = 9)

```

```{r}
dist_M27 = responses(M27)

draw.dist(dist_M27)


```


## TL-LR, Verticale

```{r}
M28a = apply(
  Raven(
    st1 = cof(square(), 
              pentagon(), 
              luck(s.x=17, s.y = 14)), 
    vrule = c("diff_shapes"), 
    hrule = c("diff_shapes")
  )
)

M28b = apply(
  Raven(
    st1 = cof(circle(s.x=8, s.y=8)), 
    vrule = c("fill", "size"), 
    hrule = c("identity")
  )
)

M28 = com(M28a, M28b)
draw(M28)
```

```{r}
dist_M28 = responses(M28)

draw.dist(dist_M28)

```


## TR-LL, + altro

```{r}
M29a = apply(
  Raven(
    st1 = cof(square(), 
              pentagon(), 
              e.hexagon(s.x = 15, s.y=15)), 
    vrule = c("diff_shapes.inv"), 
    hrule = c("diff_shapes.inv")
  )
)




M29b = apply(
  Raven(
    st1 = cof(u.biscuit, 
              circle(s.x=8, s.y=8, shd = "black"), 
              luck(shd="black")), 
    vrule = c("diff_shapes", "size"), 
    hrule = c("diff_shapes", "size")
  )
)

M29 = com(M29a, M29b)

draw(M29)


```


```{r}
dist_29 = responses(M29)

draw.dist(dist_29)
```


## Bonus

```{r}
M29c = apply(
  Raven(
    st1 = circle(s.x=8, s.y=8), 
    vrule = c("fill", "size"), 
    hrule = c("fill", "size")
  )
)

M29_bonus = com(M29a, M29c) 
draw(M29_bonus)
```
:::

```{r}
dist_M29Bonus = responses(M29_bonus)

draw.dist(dist_M29Bonus)
```


# Progressione Quantitativa

::: panel-tabset
## LL-TR (crescente orizontale e decrescente verticale)

```{r}
p001<-apply(Raven(square(shd="black")))
p001<-numeric_progression(p001,"LL-TR")
draw(p001)

```

```{r}
dist_p001 = responses(p001)

draw.dist(dist_p001)
```



## TL-LR

```{r}
p003<-apply(Raven(hexagon()))
p003<-numeric_progression(p003,"TL-LR-increasing")
draw(p003)

```
:::

```{r}
dist_p003 = responses(p003)

draw.dist(dist_p003)

```


## Forma, Progressione Quantitaiva

::: panel-tabset
## V su entrambe le regole

```{r}
p004 <- apply(Raven(cof(pentagon(),triangle(),e.hexagon()),"identity","diff_shapes"))
p004<-numeric_progression(p004,"v.increasing")
draw(p004)
```


```{r}
dist_p004 = responses(p004)

draw.dist(dist_p004)

```

## V per una regola e H per l'altra

```{r}
p005 <- apply(Raven(cof(pentagon(),luck(),e.hexagon()),"diff_shapes"))
p005<-numeric_progression(p005,"h.increasing",n=2)
draw(p005)

```


```{r}
dist_p005 = responses(p005)

draw.dist(dist_p005)

```

## H per una regola e V per l'altra

```{r}
p006 <- apply(Raven(cof(square(),luck(),triangle()),"identity","diff_shapes"))
p006<-numeric_progression(p006,"v.increasing",n=1.7)
draw(p006)

```
:::

```{r}
dist_p006 = responses(p006)

draw.dist(dist_p006)

```

## P007 

```{r}
p007 <- apply(Raven(luck(),
                  "identity","rotation"))
p007<-numeric_progression(p007,"v.increasing",n=2.5)
draw(p007)

```


```{r}
dist_p007 = responses(p007)

draw.dist(dist_p007)

```

## P008/9

```{r}
p008<- apply(Raven(e.hexagon()))
p008<-numeric_progression(p008,"v.increasing",n=1)
p009a<- apply(Raven(vline(pos.y=5, pos.x=-1,s.x=5),"rotation"))

p009 = com(p008, p009a)
draw(p009)

```


```{r}
dist_p009 = responses(p009)

draw.dist(dist_p009)
```

## P010

```{r}
p010<- apply(Raven(pentagon(),vrule = "rotation"))
p010<-numeric_progression(p010,"h.increasing",n=1.5)
draw(p010)
```


```{r}
dist_p010 = responses(p010)

draw.dist(dist_p010)
```


## Ragionamento induttivo simbolico/astratto

::: panel-tabset
## AND orizzontale

```{r}
M32<-apply(Raven(cof(vline(),square(),hline(),s_vertical()),"AND"))
draw(M32)

```

```{r}

dist_M32 = responses(M32)

draw.dist(dist_M32)
```


## AND orizzontale o verticale

```{r}

M33<-logic_rules(Raven(cof(semi.circle.inv(),
                cof(vline(pos.x=11),vline(pos.x=-11),
                    name="h.parallel",single = TRUE),
                cof(hline(pos.y=11),hline(pos.y=-11),
                    name="v.parallel",single = TRUE),
                semi.circle())),"AND")
draw(M33)

```

```{r}
dist_M33 = responses(M33)

draw.dist(dist_M33)
```


## OR orizzontale

```{r}

M34<-apply(Raven(cof(square(),lily()),"OR"))
draw(M34)

```
:::

```{r}
dist_M34 = responses(M34)

draw.dist(dist_M34)

```

# Logiche

```{r}
shapes<-shapes_list("Shapes_list-10-11-Ottavia.R")
shapes_riducible<-shapes[shapes$small&shapes$num_shapes==1,]
shapes_compose<-shapes[shapes$num_shapes>=2,]
shapes_single<-shapes[shapes$num_shapes==1,]


M35<-apply(Raven(pie.4(),"AND"))
draw(M35)




```


```{r}
dist_M35 = responses(M35)

draw.dist(dist_M35)
```


```{r}
M36<-apply(Raven(cof(vline(),square(),hline(),s_vertical()),"AND"))
draw(M36)
```

```{r}
dist_M36 = responses(M36)

draw.dist(dist_M36)
```


## M37

```{r}
M37<-logic_rules(Raven(cof(diagline(),hline(),vline(),diagline.inv())),"AND")
draw(M37)

M37<-logic_rules(Raven(cof(semi.circle.inv(),
                cof(vline(pos.x=11),vline(pos.x=-11),name="h.parallel",single = TRUE),
                cof(hline(pos.y=11),hline(pos.y=-11),name="v.parallel",single = TRUE),
                semi.circle())),"AND")
draw(M37)
```

```{r}
dist_M37 = responses(M37)

draw.dist(dist_M37)

```

